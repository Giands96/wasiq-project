package com.wasiq.inmobiliaria.domain.model.leads;

public enum LeadStatus {
    NEW,            // Recién llegado (Por defecto)
    CONTACTED,      // El agente ya le escribió o llamó
    DISCARDED,      // No interesado o Spam
}
